let x = fn(x, y) {
	if (x < 5) {
		return x * y - 5;
	} else {
		let test_asdf = fn(z, k) {
			let asdf = z * k;
			if (asdf > z - k) {
				return 5 * k;
			}
			return xx - z;
		}(x, y - x);

		return x - y * test_asdf;
	}
}
